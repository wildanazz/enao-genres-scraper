pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    echo 'Jenkins build triggered by GitHub webhook'
                }
            }
        }
    }
}