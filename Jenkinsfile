pipeline {
    agent any

    stages {
        stage('Docker Test') {
            steps {
                script {
                    sh 'docker ps -a'
                }
            }
        }
    }
}